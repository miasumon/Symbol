# Symbol
There are two data types.  They are primitive and reference.  Symbol belongs to primitive data types and is special from others.  There are many exceptional features.  It is used to hide a property in object and contains always unique value. The property related with symbol can't be iterated and deleted in object having individual quality. 
